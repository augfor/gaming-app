import { Button } from "react-bootstrap";

export function LandingButton(props) {
  return (
    <div className="mb-2">
      <Button variant="primary" size="lg">
        Acceso
      </Button>
    </div>
  );
}
